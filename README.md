# ChamCard Pro 🚌🇸🇾 (Pilot-Ready System)

نظام مؤسساتي متكامل لإدارة عمليات النقل الرقمي بمدينة دمشق، مصمم للمرحلة التجريبية (Pilot).

## 🚀 التشغيل الميداني
النظام يعمل باستخدام **Vite**. للتشغيل المباشر:
```bash
npm install
npm run dev
```

## 🔐 نظام الهوية (Auth & Validation)
- **التحقق الدولي:** يفرض النظام تنسيق E.164 (`+963...`) بشكل صارم في كافة الواجهات.
- **تشفير الجلسة:** يتم تخزين الـ JWT في `localStorage` مع مزامنتها عبر الترويسات (`Authorization: Bearer`).
- **تأمين التطبيق:** ميزة **App Lock** تتيح للمستخدم قفل التطبيق بـ PIN محلي لحماية الخصوصية.

## 🎭 الصلاحيات (Internal Role Guard)
الأدوار مخفية تماماً عن واجهة المستخدم. يتم التحكم بالوصول داخلياً بناءً على استجابة الخادم:
- `passenger`: الواجهة التقليدية للركاب.
- `driver`: واجهة المسح والتحقق (`validator`).
- `agent`: واجهة الوكيل لشحن البطاقات (`agent_terminal`).
- `admin`: صلاحيات التدقيق والتحكم الكامل.

### 📱 محاكاة الأدوار (Demo Mode)
في حال غياب السيرفر، استخدم نهايات الأرقام التالية لتجربة الأدوار:
- ينتهي بـ `2222` -> سائق
- ينتهي بـ `3333` -> وكيل
- ينتهي بـ `4444` -> مسؤول
- أي رقم آخر -> مسافر

## 🛡️ ميزات الأمان المتقدمة
1. **Device Binding:** ربط الحساب بمعرف جهاز فريد (`X-Device-ID`) لمنع سرقة الجلسات.
2. **Audit Logging:** نظام تدوير السجلات (Log Rotation) يحتفظ بآخر 200 عملية مع إمكانية التصدير بصيغة CSV محمية.
3. **Strict Routing:** يتم منع الوصول لأي واجهة حساسة حتى لو تم تغيير الرابط يدوياً، مع تحويل المستخدم للرئيسية.

## 🛠️ البناء (Production Build)
```bash
npm run build
```
سيقوم Vite بإنتاج ملفات جاهزة للنشر في مجلد `dist/` مع التخلص من كافة التبعات غير المستخدمة.